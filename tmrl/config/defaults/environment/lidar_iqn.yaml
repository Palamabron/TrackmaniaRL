# @package _global_
# Boundary lidar preset for IQN / SDSAC: same as `tm20` except reward shaping.
# Select with: environment=lidar_iqn
defaults:
  - tm20
  - _self_

environment:
  reward:
    min_seconds_before_failure: 0
    off_track_seconds_before_failure: 0.5
