# @package _global_
# LIDAR environment for IQN / SDSAC: same as `tm20` except reward shaping (no forward-progress cutoff).
# Select with: environment=lidar_iqn
defaults:
  - tm20
  - _self_

environment:
  reward:
    min_seconds_before_failure: 0
    off_track_seconds_before_failure: 0.5
