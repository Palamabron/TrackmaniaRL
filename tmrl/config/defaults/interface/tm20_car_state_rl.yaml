# @package _global_
# `car_state.TM2020RLInterface` defaults (TQC / Sophy interface presets build on this + `default`).
# Not a full `interface=` choice on its own.
interface:
  _target_: tmrl.custom.interfaces.car_state.TM2020RLInterface
  img_hist_len: 1
  gamepad: false
  grayscale: false
  resize_to: [128, 64]
  constant_penalty: 0.05
